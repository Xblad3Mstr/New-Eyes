using System.Collections;
using System.Collections.Generic;
using UnityEngine;

int amountOfGold = 20;

public class MidnightFlyer : Enemy
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
